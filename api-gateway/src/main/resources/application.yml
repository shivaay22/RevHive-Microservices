server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        -id: analytics-service
        uri: lb://ANALYTICS-SERVICE
        predicates:
          - Path=/analytics/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
