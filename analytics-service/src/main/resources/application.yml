server:
  port: 8083

spring:
  application:
    name: analytics-service
  config:
    import: "optional:configserver:"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
